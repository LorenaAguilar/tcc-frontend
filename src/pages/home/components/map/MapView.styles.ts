import { makeStyles, createStyles } from '@material-ui/core';

const MapViewUseStyles = makeStyles(() =>
  createStyles({
    container: {
      width: '400px',
      height: '400px',
    },
  })
);

export default MapViewUseStyles;
